sudo: required

language: scala
scala: 2.11.6
jdk: oraclejdk8

services:
  - docker

before_install:
  - docker run -d -p 127.0.0.1:9042:9042 cassandra:2.2

script:
  - mvn clean test install

cache:
  directories:
    - $HOME/.m2
